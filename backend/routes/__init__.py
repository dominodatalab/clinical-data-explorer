"""Flask blueprints for the backend.

Each module under `backend/routes/` exports a `bp` Blueprint that's
registered onto the app inside `backend.app.create_app()` (REFACTOR_PLAN.md
§1, step 1.5).
"""
