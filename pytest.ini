[pytest]
testpaths = tests
markers =
    external: tests that hit real external services (governance, LLM, Domino APIs) — skipped by default
    e2e: Playwright end-to-end browser tests
    mocked_integration: browser integration tests that mock backend API responses and do not start the Flask/MCP servers
